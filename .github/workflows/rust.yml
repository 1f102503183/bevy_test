on: [push, pull_request]

name: Continuous integration

jobs:
  check:
    name: Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4 # V4に更新

      # 🛠️ 複数のシステムライブラリをインストールするステップ (変更なし)
      - name: Install System Development Libraries (Wayland and ALSA)
        run: sudo apt-get update && sudo apt-get install -y libwayland-dev libasound2-dev

      # 📦 Cargoキャッシュの追加 (実行速度向上)
      - uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
            
      - uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: nightly
          override: true
          
      - uses: actions-rs/cargo@v1
        with:
          command: check
